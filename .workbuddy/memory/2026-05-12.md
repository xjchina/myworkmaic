## 2026-05-12

### Turbopack FATAL panic 修复

**问题**：启动后访问任意页面报 `An unexpected Turbopack error occurred`，终端显示 `FATAL: Failed to write app endpoint /classroom/page`。

**根因**：项目根目录存在一个名为 `nul` 的普通文件（约 181 字节，内容是之前 CMD `dir > nul` 命令误创建的错误输出）。Windows 系统保留 `NUL` 为设备名，Turbopack 的 PostCSS 在处理 `globals.css` 时路径拼接到该文件，导致 `os error 1`（函数不正确）直接 panic。

**修复**：删除 `C:\develop\open\OpenMAIC\nul` 文件即可。

**顺带改动**（非根因，但已应用）：
- `next.config.ts` → `transpilePackages` 增加 `'three'`（修复 Three.js Turbopack 兼容性）
- `components/three-lab/three-scene.tsx` → OrbitControls import 路径改为 `three/addons/controls/OrbitControls.js`
- `app/page.tsx` → `<style jsx>` 改为 `page.module.css`（CSS Module），新增 `app/page.module.css`

**诊断方法**：查看 `%TEMP%\next-panic-*.log`，Caused by 段落会直接指向问题文件。

### demo 内容调整

- `app/classroom/page.tsx`：删除"Python 编程基础·函数与模块"内置示例卡片及其专用 CSS（`.demo-card`、`.demo-badge`、`.demo-action`），保留"集合的基本概念"卡片
- `lib/data/demo-data-sets.ts`：删除两组 Quiz（`QUIZ_1_QUESTIONS`、`SCENE_QUIZ_1`、`QUIZ_2_QUESTIONS`、`SCENE_QUIZ_2`）及导出（`SETS_QUIZ_QUESTIONS`、`SETS_QUIZ_SUMMARY`），`SETS_SCENES` 只保留 6 个 slide 场景；同时移除多余的 `QuizQuestion` 类型 import

### 圆桌讨论左侧列表布局修复

- `components/workbench/roundtable-workbench.tsx`
  - `.rt-side-list`：`display: grid` → `display: flex; flex-direction: column`，避免网格拉伸导致高度随意
  - `SessionItem` div：加 `h-[52px] shrink-0 py-0`，每条记录固定 52px 高度

### 会员功能后端开发（完整实施）

**涉及文件**：
- `lib/db/schema.ts`：新增 `subscriptions`、`subscription_codes`、`usage_logs`、`share_rewards` 4 张表，`users` 表增加 `subscription_type`、`subscription_expires_at`、`invite_code`、`invited_by` 字段
- `lib/db/migrations/0000_fixed_jigsaw.sql`：自动生成的迁移文件（`npx drizzle-kit generate`）
- `lib/server/subscription.ts`：会员服务层（权限表、订阅状态、使用次数检查/记录、兑换码、邀请奖励）
- `app/api/user/subscription/route.ts`：GET 订阅状态
- `app/api/usage/check/route.ts`：GET 使用次数检查
- `app/api/usage/log/route.ts`：POST 使用记录
- `app/api/subscribe/verify-code/route.ts`：POST 兑换订阅码
- `app/api/subscribe/create/route.ts`：POST 创建订阅（支付回调）
- `app/api/promotion/upgrade-tip/route.ts`：GET 升级提示
- `app/api/share/generate-link/route.ts`：POST 生成邀请链接
- `app/api/share/stats/route.ts`：GET 分享统计
- `app/api/auth/me/route.ts`：增加 `subscriptionType`、`subscriptionExpiresAt` 字段

**会员等级**：free（0元）/ sub（月费¥29，30次/天）/ vip（年费¥199，无限次）
**认证方式**：Cookie-based（与项目其他接口一致，非 JWT Bearer）
**迁移执行**：需在有 DB 连接时运行 `node lib/db/migrate-membership.cjs`（已执行完毕）
- 注意：`ADD COLUMN IF NOT EXISTS` 是 MySQL 8.0+ 语法，旧版 MySQL 不支持，需用 information_schema 检查后再 ALTER
- 已创建 `lib/db/migrate-membership.cjs`（CJS 脚本，用 mysql2/promise 直连执行）
